# 突变证据 — 2026-08-29（首次移植）

## 门构成

- **guard 门**：`.factory/guard.py`（周界 hard-fail；self_check 核对
  factory-local.json ↔ MISSION.md 周界集合一致 + 路径存在，fail-closed exit 2）
- **tests 门**：`scripts/run_tests.sh --no-lock`
  = guard self-check + SKILL.md lint R1–R6 + pytest（载体未建挂起）

## 结果（run.py 全量）

| 维度 | 结果 |
|------|------|
| 篡改类拦截（guard） | 5/5 = **100%**（G-01…G-05；G-04/G-05 为 fail-closed exit 2 路径） |
| 行为破坏类拦截（tests） | 3/3 = **100%**（B-101/B-102/B-103） |
| 正向缺陷拦截 kill rate | **8/8 = 100%** |
| 负例放行 | 2/2（G-06/B-104：载体层良性改动不被拦——门不全拒） |

## 门缺陷发现与修复（mutations 的直接产出）

首轮 B-102 **FAIL**：lint R3 原规则为"前 40 行含 SECURITY.md 裸字样"，
SKILL.md frontmatter description 的顺带提及即可绕过——抹除正文安全纪律
链接后门仍绿。修复：R3 升级为"前 40 行必须存在指向 SECURITY.md 的
markdown 链接"（可点引用 + R4 目标存在性复验），重跑 10/10 全 PASS。

结论：**门灵敏度达标（auto-merge 的必要非充分条件；本仓 auto-merge 默认关闭）**。

## 环境

- 上游锚点：`im47cn/awesome-rules@04ee96d`（= 网关 gtsp-wop-gateway 同款基线）
- 移植差异：文档型仓库适配（triage 判据 b doc-only 属工厂范围；周界含
  docs/intent.md 判据层与 skills/wop-cli/scripts/ 私钥边界）
