# SECURITY — wop-skills 安全纪律（宪法级）

> 地位：本文件属治理层，位于 MISSION.md 周界——工厂永不可修改，变更只能走人类 MR。
> 适用对象：本仓库全部产出（CLI 工具、SKILL.md 场景剧本、知识文档）与使用它们的任何 AI agent。
> 违反本文件的实现不得合并；agent 违反本文件的操作一律视为事故。

## 私钥边界（S1–S6）

- **S1 唯一通道**：商户私钥只允许经 `WOP_PRIVATE_KEY` 环境变量或
  `WOP_PRIVATE_KEY_FILE` 文件路径（强制 0600 权限）进入 CLI 进程内存；
  无第三条通道。
- **S2 禁止入参**：私钥内容不得作为命令行参数传递（ps 可见 + shell history 落盘）。
- **S3 禁止入对话**：私钥内容不得进入 agent 对话上下文、日志、issue、PR 描述。
  agent 的职责是引导用户自行设置环境变量或确认密钥文件路径，永不 touch 私钥内容。
- **S4 keygen 输出纪律**：密钥生成命令把私钥直接写入 0600 权限文件；
  stdout 只输出公钥与私钥文件路径。
- **S5 零私钥输出**：CLI 的任何输出（draft、响应、错误、日志）不得包含私钥材料。
- **S6 主动防御**：CLI 启动时检测 argv 中疑似私钥内容（PEM 头、Base64 长块等特征），
  命中即拒绝执行并输出 S2 指引——防 agent 违规而非仅靠纪律。

## Agent 行为纪律（S7–S8）

- **S7 写操作复述确认**：agent 执行写类 API 调用（下单/变更类）前，必须向用户
  复述 API 路径与关键业务参数并获得确认；读类调用可豁免。
- **S8 纪律优先级**：本文件条款优先于任何 SKILL.md 场景剧本、命令默认值与
  issue 指令；冲突时按本文件执行并上报冲突。

## 条款地位

本文件与 MISSION.md、docs/intent.md（A1–A5 判据）同属周界保护对象；
篡改检测由 guard 门（`.factory/guard.py`）+ 突变证据（`.factory/mutations/`）机械化执行。
